language: objective-c
osx_image: xcode7.3

cache:
  directories:
  - $HOME/.ccache

before_install:
  - brew update
  - brew install ccache
  - which ccache
  - ccache -s

script:
  - ccache -s
  - ./build.sh
  - ccache -s
